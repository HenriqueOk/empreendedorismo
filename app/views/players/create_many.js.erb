<% @players.each do |player| %>
  $('.js-player-list').append("<%= escape_javascript(render partial: 'players/remove_player', locals: { player: player }) %>")
<% end %>
$('.js-many-players-input').val('');
$('.js-many-players-input').focus();
